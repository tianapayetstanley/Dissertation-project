pragma solidity >=0.8.2 <0.9.0;

contract SimpleContractOpps{
    // arithmatic + - * / % ++ --
    uint a = 2;
    uint b = 3;

    uint c = a % b

    // logical operators
    //! == NOT, && == AND, || == OR , == MEANS EQUAL != == MEANS NOT EQUL
    
    // bitwise operators
    //&, |, upwards, arrow, tidal, <<,>>
    // working on individaul bits
    bytes1 contractValue = 0x02 | 0x01 
}